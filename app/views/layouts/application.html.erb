<!DOCTYPE html>
<html>
  <head>
    <title>Arris</title>
    <script src="https://npmcdn.com/tether@1.2.4/dist/js/tether.min.js"></script>
    <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <!-- Common Header
    ================================================== -->
    <div id="header-wrapper">
      <div id="header">
        <div class="header-logo">
          <a href="https:/www.commspeed.net/"><%= image_tag('speedconnect.png', :alt => "SpeedConnect", :size => "275x90" ) %></a>
        </div>
        <div>
          <h1 class='header-wrapper'>Arris C3 System</h1>
        </div>
      </div>
    </div>

    <!-- Flash Alert/Warning, etc. Messages -->
    <% if !flash.blank? %>
      <div class='flash_message'>
        <% flash.each do |name, msg| %>
          <%= content_tag :div, msg, :id => "flash_#{name}" %>
        <% end %>
      </div>
    <% end %>

    <div class="container">
      <div class="row">
        <div class="col-md-12">
          <%= yield %>
        </div>
      </div>
    </div>
    <footer>
    </footer>
  </body>
</html>
